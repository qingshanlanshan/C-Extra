#include <stdio.h>

int main()
{
    hashmap<int,int> h=hashmap<int,int>(10);
    
    int key=1;
    int value=2;
    h.insert(key,value);
    printf("%d\n",h.get(key));
    printf("%d\n",h.size());

    h.remove(key);
    printf("%d\n",h.size());
 
    return 0; 
}
